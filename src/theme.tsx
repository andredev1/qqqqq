import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  shape: {
    borderRadius: 0,
  },
  
  palette: {
    primary: {
      main: '#000',
    },
    secondary: {
      main: '#009F51',
    },
    
  },
  
});

export default theme;